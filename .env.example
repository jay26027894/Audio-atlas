# Audio Atlas Environment Variables
# 
# This file contains example environment variables for the Audio Atlas project.
# Copy this file to .env to use it locally.
#
# Note: Audio Atlas primarily runs on-device using Chrome's Built-in AI,
# but fallback services are available when Chrome AI is not supported.

# Fallback AI Services (used when Chrome Built-in AI is unavailable)
# OpenAI API Key (fallback option 1)
VITE_OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (fallback option 2)  
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Cohere API Key (fallback option 3)
VITE_COHERE_API_KEY=your_cohere_api_key_here

# Enable mock responses when no API keys available
VITE_ENABLE_MOCK_RESPONSES=true

# Future potential environment variables:
# VITE_ANALYTICS_ID=your-analytics-id
# VITE_SENTRY_DSN=your-sentry-dsn
